
const initState = {

};

export const passwordRecoveryReducer = (state = initState, action: any): any => {
    switch (action.type) {
        case "": {
            return state;
        }
        default: return state;
    }
};

export const passwordRecoveryAC = (): any => {};