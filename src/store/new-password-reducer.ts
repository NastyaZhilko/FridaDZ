
const initState = {

};

export const newPasswordReducer = (state = initState, action: any): any => {
    switch (action.type) {
        case "": {
            return state;
        }
        default: return state;
    }
};

export const newPasswordAC = (): any => {};